# Task 1

This means that functions can be used in exactly the same way as any other objects (such as numbers or strings), and
they can be:  
assigned to variables, passed as arguments to other functions, returned as results from other functions,
and stored in containers

# Task 2

`def` is used to create named functions that can contain complex logic and return a result using `return`. `lambda`
functions, on the other hand, do not have a name of their own and must consist of exactly one expression.

# Task 3

This is when a nested (inner) function “retains” variables from the outer scope, even if the outer function has already
finished executing

# Task 4

Closures are often used for:
Creating function factories: When you need to generate functions on the fly with predefined settings.
Encapsulation: They allow you to store data (such as a call counter) without having to create a full-fledged class with
methods.  

I also found this about decorators: “The decorator architecture in Python is based precisely on closures (an outer
function takes a function, and an inner function adds new behavior to it).”

### Long live the deepl.com